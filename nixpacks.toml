[phases.setup]
nixPkgs = ["nodejs_20", "python3", "pkg-config", "cairo", "pango", "libpng", "libjpeg", "giflib"]

[phases.install]
cmds = ["npm install --omit=dev"]

[start]
cmd = "node src/index.js"
